module ReportHelper
  def get_in_complete_feedback(incomplete, complete)
    if incomplete == "N/A" && complete == "N/A"
      incomplete = "N"
      complete   = "A"
    end
    "#{incomplete}/#{complete}"
  end
end
